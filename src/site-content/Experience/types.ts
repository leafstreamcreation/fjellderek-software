export type Experience = {
    period: string;
    role: string;
    company: string;
    description: string;
    achievements: string[];
  };

export type Certification = {
    title: string;
    issuer: string;
    icon: string;
    year: number;
};